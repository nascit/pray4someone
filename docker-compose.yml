db:
  image: postgres
  environment:
    POSTGRES_USER: prayerbot
    POSTGRES_PASSWORD: danger-password

web:
  build: .
  user: root
  command: gunicorn -w 4 -b 0.0.0.0:80 -k gevent web:app --reload
  volumes:
    - ./:/code/
  ports:
    - "80:80"
  links:
    - db
